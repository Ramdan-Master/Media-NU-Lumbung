# ===========================================
# Media Organisasi Deployment Configuration
# Edit these values according to your setup
# ===========================================

# Server Configuration
SERVER_HOST="your_server_ip_or_domain"
SERVER_USER="root"  # or your aaPanel user (usually root or www)
SERVER_PATH="/www/wwwroot/your_domain.com"  # aaPanel default web root

# Database Configuration
DB_NAME="media_organisasi"
DB_USER="your_db_user"
DB_PASS="your_db_password"

# Cloudflare Configuration (optional)
CLOUDFLARE_ZONE_ID="your_cloudflare_zone_id"
CLOUDFLARE_API_TOKEN="your_cloudflare_api_token"
DOMAIN="your_domain.com"

# Local Project Path (auto-detected, don't change)
LOCAL_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
